const ErrorMessage = () => {
  return (
    <div className="error-message">
      <p>Sorry, there was an error loading the images. Please try again later.</p>
    </div>
  );
};

export default ErrorMessage;